# тестовый API (можно не настоящий, просто чтобы не ломались импорты)
VITE_API_URL=http://localhost:3001

# basename для тестов
VITE_BASENAME=/

# другие переменные, которые используются в тестах
VITE_APP_NAME=FinTrack-Test